# Copy to staging.env on the NAS (NOT committed) alongside compose.staging.yml.
#
# CADDY_PORT — the loopback port DSM Web Station reverse-proxies the staging
# hostname to. Must not collide with the other sites already on this NAS
# (chrishemmings, m-path, skovbyesexologi each hold one).
CADDY_PORT=8083
